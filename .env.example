# Copy to `.env` (git-ignored) and fill in. Only needed for docker-compose /
# production-style runs — local `quarkus:dev` against the fixture DB does not
# read this file.

# Password for the production Postgres container (docker-compose.yml's prod-db).
PROD_DB_PASSWORD=change-me
