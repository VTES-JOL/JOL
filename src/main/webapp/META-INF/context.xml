<?xml version="1.0" encoding="UTF-8"?>
<Context path="/jol">
    <CookieProcessor sameSiteCookies="lax"/>
    <!--
      Rewrites every top-level view route to /react/index.html (see
      WEB-INF/rewrite.config) — no auth check, no Java code. Auth gating is
      decided client-side by AuthGate (frontend/src/App.tsx) once the SPA
      has loaded, backed by the existing SecurityFilter-protected /nav call.
    -->
    <Valve className="org.apache.catalina.valves.rewrite.RewriteValve"/>
    <!--    <Manager className="org.redisson.tomcat.RedissonSessionManager"-->
    <!--             configPath="${catalina.base}/conf/redisson.yaml"-->
    <!--             readMode="REDIS"-->
    <!--             updateMode="DEFAULT"/>-->
</Context>
